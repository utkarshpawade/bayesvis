% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc_shrinkage.R
\name{compute_shrinkage}
\alias{compute_shrinkage}
\title{Compute shrinkage factors from posterior and prior draws}
\usage{
compute_shrinkage(x, prior_sd = NULL, prior_draws = NULL)
}
\arguments{
\item{x}{A numeric matrix of posterior draws with dimensions
[iterations × parameters]. Column names are used as parameter labels.
A data frame will be coerced to a matrix.}

\item{prior_sd}{A numeric vector of prior standard deviations, one per
parameter. Recycled to \code{ncol(x)} if scalar. Either \code{prior_sd} or
\code{prior_draws} must be supplied.}

\item{prior_draws}{An optional numeric matrix of prior draws with the same
column structure as \code{x}. Use this when closed-form prior SDs are
unavailable. If both \code{prior_sd} and \code{prior_draws} are supplied,
\code{prior_draws} takes precedence.}
}
\value{
A named numeric vector of shrinkage factors, one per parameter.
}
\description{
A helper that computes the prior-to-posterior shrinkage factor
\eqn{\kappa = 1 - \widehat{\operatorname{Var}}(\theta \mid y) /
\widehat{\operatorname{Var}}(\theta)} for each parameter, returning a
named numeric vector. Values are clamped to [0, 1].
}
\examples{
set.seed(1)
post <- matrix(rnorm(500 * 4, mean = c(0, 1, 2, 0), sd = 0.3), ncol = 4)
colnames(post) <- paste0("b[", 1:4, "]")

compute_shrinkage(post, prior_sd = 1)

}
